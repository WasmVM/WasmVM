name: main

on:
  push:
  pull_request:
    branches: [ master ]

jobs:
  build:
    runs-on: ubuntu-latest
    container:
      image: wasmvm/wasmvm:ci
    steps:
      - uses: actions/checkout@v2
      - run: mkdir -p build && cd build && cmake -G Ninja -D CMAKE_BUILD_TYPE=Release .. && ninja
      - uses: actions/cache@v2
        with:
          path:
            ${{ github.workspace }}/build
          key: ${{ github.sha }}

  test:
    runs-on: ubuntu-latest
    container:
      image: wasmvm/wasmvm:ci
    needs: build
    steps:
      - uses: actions/checkout@v2
      - uses: actions/cache@v2
        with:
          path:
            ${{ github.workspace }}/build
          key: ${{ github.sha }}
      - name: Run CTest
        run: cd build && ctest --output-on-failure